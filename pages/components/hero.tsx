import React from "react";
import style from "styles/components/hero.module.css";

function Hero() {
  return (
    <div className={`${style.hero}`}>
      <h2>城ドラ</h2>
    </div>
  );
}

export default Hero;
